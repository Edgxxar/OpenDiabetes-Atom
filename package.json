{
  "name": "open-diabetes-filter",
  "main": "./lib/index",
  "version": "0.0.0",
  "description": "Allows programming OpenDiabetesFilters using Google Blockly",
  "keywords": [],
  "deserializers": {
    "open-diabetes-filter/EditorView": "deserializeEditor",
    "open-diabetes-filter/PlotView": "deserializePlot"
  },
  "repository": "https://github.com/Edgxxar/OpenDiabetes-Atom",
  "license": "AGPL-3.0",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
    "child_process": "^1.0.2",
    "dockerode": "^2.5.8",
    "etch": "^0.14.0",
    "fs-extra": "^8.1.0",
    "md5-file": "^4.0.0",
    "pdfjs-dist": "^2.0.943",
    "uuid": "^3.3.2",
    "xml-js": "^1.6.11"
  },
  "configSchema": {
    "docker-cleanup": {
      "title": "Remove Docker containers after exection",
      "description": "Remove Docker containers after they finished executing.",
      "type": "boolean",
      "default": true,
      "order": 0
    },
    "docker-remote": {
      "title": "Use Remote Docker",
      "description": "Use a remote Docker instance. Specify host and port below.",
      "type": "boolean",
      "default": false,
      "order": 1
    },
    "docker-host": {
      "title": "Remote Docker Host",
      "description": "Protocol and host of your docker daemon.",
      "type": "string",
      "default": "http://mydockerhost.tld",
      "order": 2
    },
    "docker-port": {
      "title": "Remote Docker Port",
      "description": "Port of your docker daemon",
      "type": "integer",
      "default": 2375,
      "order": 3
    },
    "cli": {
      "title": "Command Line Interface",
      "description": "Not used if you are using Docker. Path to your OpenDiabetesVault command line interface executable",
      "type": "string",
      "default": "OpenDiabetesVault.jar",
      "order": 4
    },
    "cli-precompile": {
      "title": "Filter compilation",
      "description": "If enabled filter processes will be compiled, using the compile command below, before being passed to the CLI.",
      "type": "boolean",
      "default": true,
      "order": 5
    },
    "commands": {
      "title": "Commands",
      "type": "object",
      "properties": {
        "version": {
          "title": "CLI version",
          "description": "Used to check the version of your command line interface.<br>**Arguments:**<br>`cli`: path to your CLI executable",
          "type": "string",
          "default": "java -jar \"%cli%\" --version",
          "order": 1
        },
        "init": {
          "title": "ODV repository init",
          "description": "Used to initialize an ODV repository.<br>**Arguments:**<br>`cli`: path to your CLI executable<br>`path`: path to your current project",
          "type": "string",
          "default": "java -jar \"%cli%\" init \"%path%\"",
          "order": 2
        },
        "listtags": {
          "title": "ODV tag list",
          "description": "Used to get all tags of the current ODV repository.<br>**Arguments:**<br>`cli`: path to your CLI executable",
          "type": "string",
          "default": "java -jar \"%cli%\" tag -l",
          "order": 3
        },
        "import": {
          "title": "ODV data import",
          "description": "Used to import data into your ODV repository.<br>**Arguments:**<br>`cli`: path to your CLI executable<br>`type`: import type<br>`files`: files to import",
          "type": "string",
          "default": "java -jar \"%cli%\" import -t %type% %files%",
          "order": 4
        },
        "export": {
          "title": "ODV data export",
          "description": "Used to export all data from your ODV repository.<br>**Arguments:**<br>`cli`: path to your CLI executable<br>`type`: import type",
          "type": "string",
          "default": "java -jar \"%cli%\" export -t %type%",
          "order": 5
        },
        "exporttagged": {
          "title": "ODV data export (tagged)",
          "description": "Used to export data with the given tag from your ODV repository.<br>**Arguments:**<br>`cli`: path to your CLI executable<br>`type`: import type<br>`tag`: input tag",
          "type": "string",
          "default": "java -jar \"%cli%\" export -t %type% -i %tag%",
          "order": 6
        },
        "compile": {
          "title": "ODV filter compilation",
          "description": "Used to compile ODV filter classes before execution<br>**Arguments:**<br>`cli`: path to your CLI executable<br>`file`: relative path to process container",
          "type": "string",
          "default": "javac -cp \"%cli%\" \"%file%\"",
          "order": 7
        },
        "process": {
          "title": "ODV filter process",
          "description": "Used to execute filters.<br>**Arguments:**<br>`cli`: path to your CLI executable<br>`file`: relative path to process container<br>`out`: tag used for results",
          "type": "string",
          "default": "java -jar \"%cli%\" process -f \"%file%\" -o %out%",
          "order": 8
        },
        "processtagged": {
          "title": "ODV filter process",
          "description": "Used to execute filters.<br>**Arguments:**<br>`cli`: path to your CLI executable<br>`file`: relative path to process container<br>`in`: tag used for input<br>`out`: tag used for results",
          "type": "string",
          "default": "java -jar \"%cli%\" process -f \"%file%\" -i %in% -o %out%",
          "order": 9
        },
        "plotversion": {
          "title": "Plot version",
          "description": "Used to check the version of the plot executable.<br>**Arguments:**<br>`plot`: path to your plot executable",
          "type": "string",
          "default": "python \"%plot%\" --version",
          "order": 10
        },
        "plot": {
          "title": "Plot generation",
          "description": "Used to generate plots from data.<br>**Arguments:**<br>`plot`: path to your plot executable<br>`file`: path to the data file<br>`out`: path to output directory",
          "type": "string",
          "default": "plot -c \"%config%\" -f \"%file%\" -d -T -l -o \"%out%\"",
          "order": 11
        }
      },
      "order": 7
    }
  }
}
